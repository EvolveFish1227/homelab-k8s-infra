apiVersion: apps/v1
kind: Deployment
metadata:
  name: samba
  namespace: samba
  labels:
    app: samba
spec:
  replicas: 1
  selector:
    matchLabels:
      app: samba
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: samba
    spec:
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      initContainers:
        - name: init-samba-dirs
          image: busybox:latest
          command:
            - sh
            - -c
            - |
              mkdir -p /data/lib /data/cache /data/run /data/private /data/private/msg.sock /var/run/samba
              chmod -R 755 /data /var/run/samba
              chmod -R 700 /data/private
          volumeMounts:
            - name: data-dir
              mountPath: /data
            - name: samba-var-run
              mountPath: /var/run/samba
      containers:
        - name: samba
          image: crazymax/samba:latest
          env:
            - name: TZ
              value: UTC
            - name: SAMBA_LOG_LEVEL
              value: "1"
            - name: SAMBA_WORKGROUP
              value: "WORKGROUP"
            - name: SAMBA_SERVER_STRING
              value: "Homelab Samba Gateway"
            - name: WSDD2_ENABLE
              value: "1"
            - name: AVAHI_ENABLE
              value: "0"
          ports:
            - name: smb
              containerPort: 445
              protocol: TCP
          volumeMounts:
            - name: data-dir
              mountPath: /data
            - name: config
              mountPath: /data/config.yml
              subPath: config.yml
            - name: storage
              mountPath: /samba/storage
            - name: samba-var-run
              mountPath: /var/run/samba
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          securityContext:
            runAsUser: 0
            capabilities:
              add:
                - CAP_CHOWN
                - CAP_FOWNER
                - CAP_SETGID
                - CAP_SETUID
      volumes:
        - name: data-dir
          emptyDir: {}
        - name: config
          configMap:
            name: samba-config
        - name: storage
          hostPath:
            path: /mnt/storage
            type: Directory
        - name: samba-var-run
          emptyDir: {}
